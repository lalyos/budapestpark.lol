{
  "name": "bp-park-events",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply bp_park_db --local",
    "db:migrate:remote": "wrangler d1 migrations apply bp_park_db --remote"
  },
  "dependencies": {
    "hono": "^4.8.3"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.19.1",
    "@cloudflare/workers-types": "^4.20260604.0"
  }
}
